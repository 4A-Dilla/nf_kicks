import 'package:flutter/material.dart';

const kActiveButtonBorderColor = RoundedRectangleBorder(
    borderRadius: const BorderRadius.vertical(bottom: Radius.circular(10)));

const kInactiveButtonBorderColor = RoundedRectangleBorder(
    borderRadius: const BorderRadius.vertical(bottom: Radius.circular(0)));

const kActiveSliderButtonBorderColor = RoundedRectangleBorder(
    borderRadius: const BorderRadius.all(Radius.circular(10)));

const kInactiveSliderButtonBorderColor = RoundedRectangleBorder(
    borderRadius: const BorderRadius.all(Radius.circular(10)));

const Color kActiveBtnColor = Colors.deepOrange;
const Color kInactiveBtnColor = Colors.white;

const Color kActiveBtnTextColor = Colors.white;
const Color kInactiveBtnTextColor = Colors.black;
